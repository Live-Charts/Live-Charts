<UserControl x:Class="ChartsTest.Line_Examples.AnimationImprovement.AnimationImprovementLine"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveCharts;assembly=LiveCharts"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock>See how line dances and moves with you!</TextBlock>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button Width="100" Click="MoveOnClick">Move All</Button>
            <Button Width="100" Click="AddSeriesOnClick">Add Series</Button>
            <Button Width="100" Click="AddPointOnClick">Add Point</Button>
        </StackPanel>
        <lvc:LineChart Grid.Row="2" LineType="Polyline" Series="{Binding Series}" />
    </Grid>
</UserControl>
