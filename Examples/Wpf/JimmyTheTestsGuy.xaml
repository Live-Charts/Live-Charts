<UserControl x:Class="Wpf.JimmyTheTestsGuy"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="300"
             d:DesignWidth="500"
              d:DataContext="{d:DesignInstance local:JimmyTheTestsGuy}">
    <Grid>
        <lvc:GeoMap Name="GeoMap" LandClick="GeoMap_OnLandClick"></lvc:GeoMap>
        <!--<lvc:CartesianChart>
            <lvc:CartesianChart.Series>
                <lvc:StepLineSeries Values="1, 4, 2, 5, 6, 8, 2" />
            </lvc:CartesianChart.Series>
        </lvc:CartesianChart>-->
        <TextBlock Margin="10 50" VerticalAlignment="Top" HorizontalAlignment="Left">This is only a preview</TextBlock>
        <Button Background="#55ffffff" Height="30"  Width="80" Margin="10" Foreground="White" FontWeight="Bold"
                VerticalAlignment="Top" HorizontalAlignment="Left" Click="ButtonBase_OnClick">Restart</Button>
    </Grid>
</UserControl>
