<UserControl x:Class="Wpf.JimmyTheTestsGuy"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="500"
             d:DesignWidth="800"
              d:DataContext="{d:DesignInstance local:JimmyTheTestsGuy}">
    <UserControl.Resources>
        <local:TetsConverter x:Key="Converter"></local:TetsConverter>
    </UserControl.Resources>
    <Grid>
        <Button Height="50" VerticalAlignment="Top" Click="ButtonBase_OnClick" Panel.ZIndex="1">Que Pedo</Button>
        <ScrollViewer>
          
                <ItemsControl Name="Control" ItemsSource="{Binding Source}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <lvc:CartesianChart Name="Chart" Height="400" 
                                            Series="{Binding PresentationTraceSources.TraceLevel=High}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
